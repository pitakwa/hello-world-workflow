name: Hello World Workflow

on: push

jobs:
  hello-world:
   runs-on: ubuntu-latest

   steps:
     - name: print Hello World
       run: echo "hello World" && sleep 30
       - name: print hello new era solution
         run:  print "hello new era solution"
